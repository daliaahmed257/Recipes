<!DOCTYPE html>
<html>
  <head>
    <title>The Soup Pot</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lilita+One&family=Nunito:ital,wght@0,300;0,400;0,500;1,400&family=PT+Sans&display=swap" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <header>
      <a class="title" href="/recipes"><h1>The Soup Pot</h1></a>
      <div class="navigation">
        <a href="/recipes">All Soups</a>
        <% if (user) { %>
            <a href="<%= `/user/${user._id}/favorites` %>">
                Favorites</a>
            <a href="/logout">Sign Out</a>
            <% } else { %>
                <a href="/auth/google" class="login">Sign In&nbsp;</a>
        <% } %>
      </div>

    </header>
